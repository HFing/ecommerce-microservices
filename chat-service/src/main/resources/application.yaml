server:
  port: 8085

spring:
  application:
    name: chat-service

  data:
    mongodb:
      connection-string: mongodb://root:root@localhost:27017/chat-service?authSource=admin


eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
